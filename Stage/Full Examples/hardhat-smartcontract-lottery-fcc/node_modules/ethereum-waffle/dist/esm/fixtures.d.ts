import { providers, Wallet } from 'ethers';
declare type Fixture<T> = (provider: providers.Provider, wallets: Wallet[]) => Promise<T>;
export declare const loadFixture: <T>(fixture: Fixture<T>) => Promise<T>;
export declare function createFixtureLoader(overrideProvider?: providers.Web3Provider, overrideWallets?: Wallet[]): <T>(fixture: Fixture<T>) => Promise<T>;
export {};
